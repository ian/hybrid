import { defineConfig } from "vocs"

export default defineConfig({
	sidebar: [
		{
			text: "Introduction"
		}
	],
	theme: {
		colorScheme: "dark",
		accentColor: "#FF5C00"
		// variables: {
		// 	fontFamily: {
		// 		default: "Inter",
		// 		mono: "Inter"
		// 	}
		// }
	},
	title: "Hybrid"
})
